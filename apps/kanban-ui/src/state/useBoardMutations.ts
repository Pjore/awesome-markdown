/**
 * @deprecated Mutations are now handled directly via provider.patchItem / provider.createItem.
 * Kept as a stub to avoid breaking the module graph during migration.
 */
export type BoardMutations = Record<string, never>;
