/**
 * @deprecated Use useBoardRender instead.
 * Kept as a stub to avoid breaking the module graph during migration.
 */
export type { BoardRenderState as BoardBySlugState } from './useBoardRender.js';
